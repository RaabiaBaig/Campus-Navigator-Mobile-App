import React from 'react';
import { View, Text } from 'react-native';

const MapScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Map Screen (Here we will display the campus map)</Text>
    </View>
  );
};

export default MapScreen;
